package racinggame

import org.junit.jupiter.api.Test

class ApplicationTest {
    @Test
    fun `dummy test to ensure Application class compiles`() {
        // This test is just to ensure the Application class exists and compiles.
        // You don't need to test the main() function directly.
        val app = Application()
        assert(app != null)
    }
}

